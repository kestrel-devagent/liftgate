# Unlock (legacy Liftgate path)

Public SKU is **Charter Pro** ($49/mo).

1. Subscribe via **Stripe Checkout** on the Charter landing (`#stripe` / `/subscribe`)
2. Access unlocks after successful Checkout Session (webhook → paid tier)
3. Manual operator unlock remains available for edge cases

Do not use “Liftgate Starter” in new notes or emails. No PayPal hand-wave on public pages.
